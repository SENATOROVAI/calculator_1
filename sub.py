"""Модуль содержит функцию для сложения."""


def subtraction(num_1: int, num_2: int) -> int:
    """
    The function `subtraction` is incorrectly named as it
    actually performs addition of two numbers. - Заголовок (title)

    description: полное описание функции

    Args:
      num_1 (int): It looks like there is a mistake in the function.
      The function is currently adding
    `num_1` and `num_2` instead of subtracting them.
    To fix this, you should change the operation from
    addition to subtraction. Here is the corrected version of the function:
      num_2 (int): It looks like there is a mistake in the function.
      The function is currently adding
    `num_1` and `num_2` instead of subtracting them. To fix this,
    you should change the operation from
    addition to subtraction. Here's the corrected version of the function:

    Returns:
      The function is currently returning the sum of the
      two input numbers (num_1 + num_2) instead of
    the subtraction.

    Example:
    >>> subtraction(1, 2)
    3
    """
    return num_1 - num_2
